{
  "name": "lune-soleil",
  "version": "0.1.0",
  "private": true,
  "description": "Boutique e-commerce de bijoux artisanaux faits main",
  "engines": {
    "node": ">=22.12.0 <23 || >=24 <25"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "type-check": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:demarrer": "docker compose up -d db",
    "db:arreter": "docker compose stop db",
    "db:preparer": "./scripts/preparer-base-locale.sh",
    "db:reinitialiser": "./scripts/preparer-base-locale.sh --reinitialiser",
    "db:verifier": "./prisma/sql-manuel/verifier-schema.sh --base-migree",
    "db:verifier:conception": "./prisma/sql-manuel/verifier-schema.sh",
    "db:console": "docker compose exec db psql -U lunesoleil -d lunesoleil",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^7.9.1",
    "next": "16.2.12",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "dotenv": "^17.4.2",
    "eslint": "^9",
    "eslint-config-next": "16.2.12",
    "eslint-config-prettier": "^10",
    "prettier": "^3",
    "prisma": "^7.9.1",
    "typescript": "^5"
  },
  "comments": {
    "overrides": [
      "Trois avis de securite sur des dependances transitives, corriges en amont mais non encore repercutes par les paquets parents. `npm audit fix --force` proposait de retrograder ESLint en v4 et Next.js en 14, ce qui casse tout : ces overrides ciblent la version corrigee sans toucher aux versions majeures.",
      "minimatch : la branche 3.x recoit brace-expansion 5 dont l'API a change, ce qui casse ESLint avec `expand is not a function`. Overrider brace-expansion seul reproduit ce defaut. C'est minimatch qu'il faut monter en 10.x, qui appelle la nouvelle API correctement.",
      "sharp : Next.js installe 0.34.5, vulnerable via libvips (CVE-2026-33327, 33328, 35590, 35591). sharp traite les photographies televersees par l'administratrice, ce chemin n'est pas seulement de l'outillage.",
      "A retirer des que les paquets parents publient des versions a jour. `npm audit` doit rester a zero."
    ]
  },
  "overrides": {
    "minimatch": "^10.2.6",
    "postcss": "^8.5.25",
    "sharp": "^0.35.3"
  }
}
